<%= turbo_frame_tag "total_amount" do %>
  <div>
    <% #TODO: 後で、ユーザーに紐づく total_amount を表示する %>
    <%= total_amount || 0 %>
  </div>

  <div>
    <% #TODO: 後で、ユーザーに紐づく total_amount を表示する %>
    <%= total_amount || 0 %>
  </div>
<% end %>
